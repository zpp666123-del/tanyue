# 弹阅 0.2.5

- 关闭弹窗仍会接续下一段，浏览记录与确认读完分开统计，未确认全部片段时不显示读完。
- TXT、Markdown 与手动 JSON 导入增加预览、样例与异常详情；保存成功才提示入库，失败保留重试。
- 设置页支持从完整数据备份恢复，校验原文哈希、结构和引用；恢复与重置初始内容前自动备份，失败保留原数据。
- 下一次提醒排除安静时段；轮询跨越休眠或时钟回拨时不补弹，也不提前弹出。
- 删除固定阅读天数和虚构打卡日期，分钟数明确为估算。
- 更新下载累计真实字节，下载与安装分离，点击安装前等待数据落盘。

本地 Windows Alpha 安装包。未生成新的公开自动更新清单；不包含开发机的用户数据或密钥。
