# 弹阅 v0.2.3 测试版说明

发布日期：2026-08-31

## 本版重点

- 首次安装内置完整《道德经》、五卷正文版《毛泽东选集》和《毛主席语录（正文版）》；所有内容从未读状态开始。
- 《毛主席语录》按 33 章、427 条正文整理为 528 个微阅读片段，删除出处、日期、卷页、编者标注和正文注号，覆盖率 100%。
- SQLite 升级到 schema v2；旧的含出处语录种子只迁移一次，替换后保持当前阅读目标不变。
- Windows 构建安装 `tanyue.exe` 与 `tanyue-cli.exe`，并生成 SHA-256 校验文件。
- 书皮改为显示更多真实书名；毛选卷册在书皮上直接显示卷次。
- 前端 48/48、Rust 18/18 测试通过；内容种子升级前会自动备份完整旧状态。

## 测试版已知限制

- 安装包尚无可信 Authenticode 签名。
- 持续输入不抢焦点、休眠两小时恢复、多显示器/DPI、托盘退出和开机启动清理仍需 Windows 人工验收。

内置《毛泽东选集》《毛主席语录》的公开再分发授权已由项目所有者确认。0.2.3 作为公开 Alpha 测试版发布，不标为稳定正式版。

---

# 弹阅 v0.2.0 发布说明

发布日期：2026-08-27

## 本版重点

- 产品名称正式统一为“弹阅”。
- 主界面重构为更简洁、低干扰、留白充足的苹果风桌面界面。
- 增加可拖拽的轻量悬浮图标，作为桌面阅读入口。
- 浏览器预览中的悬浮图标支持拖动，并在本地记忆位置。
- Tauri 桌面端增加独立 `reading-floating` 透明置顶窗口。
- 微读弹窗统一为四个动作：继续阅读、读完下一段、稍后提醒、今天暂停。
- 自动收起默认跟随片段预计阅读时长，也可切换为 15～90 秒固定时间。
- 读完末段不再循环回第一段；主窗口与悬浮入口保持互斥，避免彼此遮挡。
- “继续阅读”只展开完整上下文，不再错误地把当前片段记为读完。
- 独立微读弹窗可通过跨窗口事件打开主窗口中的对应连续阅读正文。
- 更新品牌图标、真实运行截图、产品文档和本地 AI 接力说明。

## 从旧版状态迁移

前端会优先读取新的本地状态键：

```text
tanyue.state.v1
```

若只找到旧版状态键：

```text
shuchuang.state.v1
```

系统会读取旧数据并写入新键。正式 SQLite 迁移仍属于下一阶段工作。

## 已验证

- TypeScript 严格检查。
- 32 项导入、拆分、调度、弹窗尺寸、阅读状态和《道德经》完整性测试。
- 静态构建与关键文件完整性检查。
- 主界面、微读弹窗、悬浮图标和连续阅读面板的编译产物渲染。
- PNG、ICO、ICNS 图标文件格式。

## 仍需真机完成

- Windows 与 macOS 的透明悬浮窗、置顶和多显示器位置。
- 弹窗自动出现时不抢 Word、Excel、浏览器或 IDE 的输入焦点。
- 系统托盘、开机启动、休眠恢复和安装器。
- 签名、自动升级和发布流水线。
