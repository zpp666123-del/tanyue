# 弹阅当前工程状态

更新时间：2026-08-31
版本：0.2.3

## 总体结论

当前版本属于：

> **可安装的 Windows Alpha + SQLite 本地持久化 + Agent 标准导入闭环。**

它已经具备可重复通过的前端与 Rust 工程门禁、TXT/Markdown 原生适配器、外部 Agent 标准导入协议与 CLI、可证明的 100% 正文覆盖链路、SQLite schema/迁移以及实际可安装的 NSIS x64 包。首版不再分别内置 EPUB/PDF/DOCX/URL 解析器：这些来源由用户选择的 Agent 转换为 `.tanyue.json`，弹阅只负责严格复核、入库、呈现和进度。内置《毛泽东选集》《毛主席语录》的公开再分发授权已由项目所有者确认；整体仍处于 **Windows Alpha**，签名升级以及休眠、多屏、不抢焦点完整验收尚未完成，因此当前公开版本仍标为测试版。

## Agent 导入边界已收口

- 定义版本化 `.tanyue.json` 协议与 JSON Schema，使用 Unicode 标量偏移，避免中文、emoji 和跨语言 Agent 的字符范围歧义。
- 导入包必须包含原文块、连续片段范围、来源锚点、解析器版本和拆分规则版本；Agent 不能直接写 SQLite。
- TypeScript 领域层会从原文块重新构造 `Segment.originalText`，重算内容哈希、顺序和 100% 覆盖率，Agent 提供的文本不能覆盖原文。
- Rust CLI 提供 `schema`、`validate`、`import` 三个命令；`import` 只把通过初检的包原子投递到应用 inbox。
- 运行中的弹阅每两秒接收一次 inbox；通过自身复核后保存并回执，失败包移入 `rejected` 并保留错误原因。
- 多窗口状态广播带实例来源，保存窗口忽略自身回声，连续投递多本书时不会再被异步旧快照覆盖；其他窗口仍正常同步。
- 安装包同时安装 `tanyue.exe` 与 `tanyue-cli.exe`，无需专用 Skill、MCP Server、在线书库或新增服务进程。
- 主界面导入入口只保留 TXT、Markdown 和 Agent JSON；旧的 URL 抓取演示路径已删除，避免形成两套不完整能力。

## 核心阅读体验逻辑已收口

- 弹窗默认按当前片段预计阅读时长加轻量缓冲自动收起，短片段可低于 30 秒；仍可切换 15～90 秒固定时间。右上角关闭或自然倒计时结束会把该片段计入已读进度并推进到下一段，“稍后提醒”和“今天暂停”则保留当前阅读位置。
- 弹窗四个主操作统一为继续阅读、读完下一段、稍后提醒、今天暂停；收藏改为章节进度旁的极小心形入口，不再占用底部主操作位。
- 倒计时进度统一使用本次弹窗最终采用的真实时长计算，并通过 `scaleX` 平滑递减；自适应阅读时长与固定时长不再混用分母。
- 延后和今天暂停使用统一状态计算，首页、顶栏和计划页显示真实恢复时间，并可立即恢复。
- 安静时段从隐藏默认值改为计划页可见、可编辑的设置。
- 末段确认后不再循环到第一段；当前书完成后自动准备其他书的首个未读片段，没有未读内容时不再自动重复弹出。
- 调度槽位写入持久化去重标记，避免应用在同一触发窗口内重启后重复提醒。
- 主窗口显示时隐藏桌面悬浮入口；主窗口隐藏到托盘后再恢复，避免置顶图标遮挡应用自己的主要操作。
- 设置页提供自适应、紧凑、小窗三个占屏上限；尺寸仍按正文自适应，超出上限时只滚动正文区。
- 阅读字号改为 50%～150% 的明确范围，设置页滑块同时显示“极小 / 标准 / 大”参照；拖动时通过轻量窗口事件即时更新独立弹窗和连续阅读正文，松手后持久化，弹窗随字号重新测量而不改变当前倒计时和阅读进度。
- 阅读排版会随字号自动调整：正文行高从 50% 字号的 1.92 平滑收敛到大字号的 1.66，极小字号时内容区留白同步收紧至标准的 75%，段间距继续按字体比例缩放且不叠加旧外边距；辅助标题设上下限，正文保持视觉主角；自适应弹窗在大字号短片段下适当增宽，紧凑和小窗上限不变。
- 阅读界面不再把正文首句生成的 `helperTitle` 当作标题，也不再展示“接着上一段”之类模板桥接小字；只有能与书名、页码/行号占位和正文开头区分开的真实章节标题，才在该章节首次出现时展示一次，弹窗与连续阅读使用同一判定。
- 弹窗“继续阅读”的跨窗口切换由主窗口单点编排：弹窗不再重复广播状态或提前显示主窗口，主窗口先读取最新状态、挂载连续阅读层后再显示；活动阅读层会在后续状态刷新中被立即恢复，不再闪现后退。
- 主界面高频点击不再触发控件缩放、悬浮位移或整页 `page-in` 位移动画；重复点击当前导航不重渲染，同一页面刷新保留滚动位置。
- “今日”页从 `currentSegmentId/activeBookId` 明确展示正在阅读的书名、章节和段号，只保留当前正在阅读的一本书；侧栏引用同步显示书名。
- 书架与首页复用同一个 `currentBook()` 判定：当前书使用“正在读”徽标与轻量强调，并在最近阅读、最近导入、阅读进度和书名四种排序下始终固定置顶；其余书籍的操作明确标为“切换阅读”。
- 每本书通过 `resumeSegmentForBook()` 保留独立接续位置：未确认的已展示片段继续停留，已确认后进入下一段，全书读完才从第一段重读。书架卡片直接显示将从第几段、哪个章节继续；非当前书先进入位置选择，不再无提示切换。
- 书籍详情明确展示“当前阅读位置”或“这本书的接续位置”，同时说明当前正在读的其他书；支持按章节名或段号搜索并选择任意片段。点击片段会先持久化为当前阅读位置，保存成功后才打开正文。
- 书架为每本书提供明确管理入口；删除前展示片段、收藏和笔记影响，确认后事务性清理该书及关联阅读数据，删除最后一本后进入可重新导入的空书架。
- 移除未实现的全屏/勿扰/会议暂停、应用内 AI Provider、可变关闭到托盘等假设置；关闭主窗口固定为隐藏到托盘。

## Windows 工程门禁与正式文本导入基础

- 修复 Node ESM URL 在 Windows 上生成重复盘符的问题，`npm run test`、`npm run build` 和 `npm run verify` 均可正常执行。
- 新增 npm lockfile，固定本轮实际安装的完整依赖树。
- 建立 `SourceAdapter` 接口、适配器选择和明确错误码。
- TXT/Markdown 通过正式适配器输出 `NormalizedDocument`、有序正文块、行号锚点、块哈希与解析器版本。
- 拆分器升级为 2.0：超长正文使用精确字符区间拆分，可验证无空洞、无重叠、无乱序并完整重建标准化原文。
- 每次导入生成 `CoverageReport`；无效报告在 `store.ts` 被强制拒绝，不能写入书架。
- 每个新片段记录解析器、拆分器、规则、AI 提示词和模型状态；未调用 AI 时显式为 `null`。
- 旧 LocalStorage 与演示片段回填 `legacy-unversioned` 追溯信息，不改变进度、收藏或笔记。
- 测试升级为 48 个具名用例，包含 20 万字单块文本 100% 覆盖、首页单一当前书、书架排序下当前书固定置顶、每本书独立接续位置、内置内容版本迁移、弹窗尺寸与停留时间自适应、大字号行宽与小窗上限、真实倒计时进度、50%～150% 字号范围、行高/留白联动、书皮真实书名分行、真实标题去重与旧值收敛、末段边界、暂停状态、书籍删除边界和多窗口状态广播来源验证。

## v0.2 本轮新增

- 产品正式统一命名为“弹阅”。
- 主界面改为浅色、留白、低干扰的苹果风格。
- 今日页精简为阅读摘要、继续阅读、阅读计划和今日时间线。
- 增加可拖拽、可记忆位置的浏览器悬浮图标。
- 增加 Tauri `reading-floating` 独立置顶透明窗口源码。
- 悬浮图标收敛为纯品牌入口，不再显示含义不明确的进度环或未读角标。
- 阅读弹窗改为四个明确动作：继续阅读、读完下一段、稍后提醒、今天暂停。
- 阅读弹窗在章节序号旁增加 12px 收藏心形；点击即时持久化，已收藏时填充显示，同时保留 24px 可点击区域和键盘焦点样式。
- 阅读弹窗右上角外跳图标固定表示“打开弹阅首页”：切到“今日”页、显示主窗口并收起弹窗；底部“继续阅读”才进入连续阅读页，两种语义不再复用同一动作。
- 倒计时条从当前真实停留时长的 100% 连续递减到 0%，不再因自适应时长与设置值不同而跳变。
- 独立弹窗在首次渲染前订阅跨窗口状态，避免收藏操作用旧快照覆盖主窗口刚写入的数据。
- 移除所有高频按钮按下缩放、按钮/书卡悬浮位移和页面重建入场动画，解决一次点击连续发生“按压位移 + 页面位移”的双重抖动；模态框、阅读抽屉和真实拖拽仍保留必要的空间过渡。
- 修正“展开连续阅读”不应自动确认当前片段已读的状态语义。
- 增加独立弹窗向主窗口同步状态并跨窗口打开对应连续阅读正文。
- 微读弹窗按正文长度和实际内容高度自适应，移除透明大边框；标题区、边缘与独立悬浮图标均支持拖动。
- 微读弹窗改为隐藏创建，完成最终宽高测量后才一次性显示，避免先闪现初始化大窗口再缩小。
- 今日页移除重复侧栏偏移，使用侧栏后的全部可用宽度；桌面端只在内容真实超出可用高度时滚动。
- 更新应用 SVG、PNG、ICO 和 ICNS 图标。
- 新增 `src/apple.css` 视觉覆盖层与 v0.2 UI 设计说明。

## 完成度矩阵

| 能力 | 当前状态 | 说明 |
|---|---|---|
| 极简苹果风主界面 | 已完成源码 | 今日页与全局壳已落地，其他页面继承统一视觉变量 |
| 五个主页面 | 已完成 | 今日、书架、阅读计划、收藏、设置 |
| 书架管理与删除 | 已完成 | 独立管理入口、二次确认、关联进度/收藏/笔记清理、空书架恢复导入 |
| 悬浮图标 | 已完成并真机验证 | 浏览器可拖拽；Tauri 独立窗口使用 52×52 原生有效区域，可显示、拖动和恢复 |
| 独立阅读弹窗 | 已完成 | 四操作、真实倒计时进度、轻量收藏、悬停暂停、自动收起和三档占屏上限 |
| 自动关闭 | 已完成 | 默认按预计阅读时长自适应，支持 15～90 秒固定值；自动关闭记录展示/收起并推进游标，不伪装成主动确认 |
| 展示/确认分离 | 已完成 | `shown` 与 `confirmed` 独立 |
| 连续阅读侧栏 | 已完成 | 目录、前后段、笔记、收藏 |
| SourceAdapter 基础 | 已完成 | 统一接口、选择、错误码、版本和生命周期；当前注册 TXT/Markdown |
| TXT/Markdown 导入 | 已完成正式链路 | 本地解析、标准化正文块、行号锚点与解析器版本 |
| Agent `.tanyue.json` 导入 | 已完成并端到端验证 | 版本化协议、Schema、双层校验、原子 inbox、失败回执与确定性去重 |
| Agent CLI | 已完成并随安装包交付 | `schema`、`validate`、`import`；无需 Skill 或 MCP |
| 旧约定 JSON 导入 | 兼容保留 | `{title,text}` 与弹阅示例数据，仅用于简单本地文本 |
| 规则拆分 | 已完成 2.0 | 标题、段落、目标秒数、精确原文字符区间与版本化 |
| 内容完整性检查 | 已完成文本链路 | 100% 覆盖、空洞、重叠、乱序、空片段和重复内容报告；写入前强制校验 |
| 片段处理追溯 | 已完成文本链路 | 解析器、拆分器、规则和 AI 未应用状态；旧数据明确回填 |
| 《道德经》示例 | 已完成完整 81 章 | 顺序、空段与指纹自动测试通过 |
| 阅读计划 | 已完成 | 工作日、每日数量、多时间窗口、可见安静时段、自适应/固定停留时长 |
| 延后和今天暂停 | 已完成 | 状态持久化、跨页面显示恢复时间并可立即恢复 |
| 本地进度、收藏、笔记 | 已完成 | 桌面 SQLite；浏览器预览保留 LocalStorage 回退 |
| 导出 | 已完成 | 收藏 Markdown、全部数据 JSON |
| 明暗主题与字号 | 已完成 | 系统/浅色/深色、宋体/黑体、50%～150% 即时同步字号与自动排版密度、减少动画 |
| 系统托盘 | Windows 基础验证通过 | 关闭主窗口后进程驻留；托盘菜单退出仍需人工验收 |
| 开机启动 | 已搭建源码 | Tauri Autostart 权限和桥接已配置 |
| 原生通知 | 已搭建源码 | 默认关闭，避免重复打扰 |
| SQLite | 已完成 schema v2 | 事务保存、严格校验、v1→v2 内容种子版本迁移、迁移备份、幂等导入和浏览器回退 |
| EPUB/PDF/DOCX/URL | 首版明确外置 | 由外部 Agent 解析为标准包；弹阅不重复建设多格式解析器 |
| 在线书库/API | 非目标 | 不做内容下载站；Agent 可在用户授权范围内自行取得合法来源 |
| Skill/MCP Server | 当前不需要 | 单次文件转换与 CLI 投递已经闭环，避免增加维护面 |
| 应用内 AI Provider | 已移出首版 UI | 复杂解析与辅助内容统一由外部 Agent 负责，避免展示未实现开关 |
| 全屏/勿扰检测 | 未实现 | 需 Tauri/Windows 原生能力 |
| 休眠恢复 | 调度模型已考虑 | 需桌面事件接入和真机测试 |
| Windows 安装包 | 已完成 NSIS x64 | 已构建并安装；尚未代码签名和自动升级 |

## 本环境已验证

- Node.js 24.15.0 环境执行 `npm install` 成功，审计结果为 0 个已知漏洞。
- `npm run verify` 在 Windows 成功通过，包含 TypeScript 严格检查、48/48 具名测试、静态构建与工程结构检查。
- Rust 1.98.0 stable-msvc、Cargo 1.98.0、rustfmt 1.9.0 与 Visual Studio Build Tools 2022 17.14.39 已安装。
- `cargo fmt --check`、`cargo test` 和 18/18 Rust 单元测试通过（含 Agent 包 Unicode 覆盖、原子投递与回执、SQLite v1→v2 迁移、内容种子升级前自动备份、空书架、悬浮窗口有效区域、窗口联动和高 DPI 几何测试）。
- NSIS x64 安装包已构建并成功安装；Windows 卸载注册表中仅保留当前版本安装记录，旧安装目录已由卸载器移除。
- 2026-08-28 已重新构建并覆盖安装核心体验收口版；安装器退出码为 0，安装后的应用已重新启动。
- 2026-08-28 已覆盖安装弹窗占屏约束版；旧数据默认保持自适应，用户可在设置 → 阅读外观切换紧凑或小窗。
- 2026-08-28 已覆盖安装书架管理版：书籍可进入管理并二次确认删除，关联进度/收藏/笔记同步清理，SQLite 支持合法空书架；安装后《毛泽东选集 第一卷》及 1357 个片段保持完整。
- 2026-08-28 已覆盖安装悬浮窗口区域修正版：Windows 虽将外层 HWND 最小宽度强制扩为 131px，但原生可见和命中区域被严格裁为 52×52，左右透明部分不再占位、拦截点击或参与弹窗锚定；安装后《毛泽东选集 第一卷》及 1357 个片段保持完整。
- 2026-08-28 已覆盖安装首次展开定位与多包导入竞态修正版：弹窗先设置并读取 Windows 实际外框尺寸，再按悬浮图标有效区域定位；安全净距为 4px。状态广播忽略自身回声，五个 CLI 包可在一次轮询中连续落库而不互相覆盖。
- 2026-08-28 已覆盖安装弹窗进度与轻量收藏修正版：倒计时使用实际自适应时长平滑递减，章节序号旁提供极小心形收藏；弹窗窗口在渲染前接收跨窗口状态。安装器退出码为 0，`D:\弹阅` 仅一条 0.2.0 卸载记录且仅一个运行进程。
- 2026-08-28 已覆盖安装全局交互防抖版：移除高频点击的两段位移动效，当前导航不做无效刷新，同页重绘恢复滚动位置；构建和安装器退出码均为 0。安装后仍为一个运行进程、一条 0.2.0 卸载记录，8 本书和 7643 个片段保持完整。
- 2026-08-28 已覆盖安装弹窗首页入口修正版：右上角外跳图标打开“今日”首页，底部“继续阅读”保留连续阅读语义；安装器退出码为 0，仍为一个运行进程和一条 0.2.0 卸载记录。
- 2026-08-28 已覆盖安装首页当前阅读标识版：明确显示当前书名、章节、段号并把当前书排在第一张；安装器退出码为 0，仍为一个运行进程和一条 0.2.0 卸载记录。
- 2026-08-28 已覆盖安装阅读时长口径同步版：阅读计划中的“当前片段预计阅读”与弹窗共用 `segment.estimatedSeconds` 和同一格式化函数，当前片段如预计 33 秒会在两处一致显示；30/60/90 秒仅作为“新内容每段目标时长”保留在导入流程并持久化，不再误导为当前片段时长。自适应收起继续按当前片段预计时长加 6 秒轻量操作余量计算。安装后 8 本书、7643 个片段保持完整。
- 2026-08-28 已覆盖安装阅读字号真实联动版：修复设置页原先修改未被阅读正文使用的变量、独立弹窗收到状态后不刷新的两处断链；字号范围扩大为 80%～150%，拖动时弹窗即时变化并重新测量，连续阅读与重启后的设置保持一致。安装器退出码为 0，`D:\弹阅` 仅一个运行进程，8 本书和 7643 个片段保持完整。
- 2026-08-28 已覆盖安装大字号排版收敛版：移除正文默认段落外边距叠加，统一段落节奏；行高随字号自动从 1.84 收敛到 1.66，辅助标题限制最大尺寸，大字号自适应弹窗增加行宽但不突破用户选择的占屏上限。安装器退出码为 0，`D:\弹阅` 仅一个运行进程，8 本书和 7643 个片段保持完整。
- 2026-08-28 已覆盖安装 50% 极小字号版：字号范围扩展为 50%～150%，极小字号行高自动放宽到 1.92、内容区留白收紧到标准的 75%，标题保留 14px 下限，段落间距按字号比例缩放且清除旧规则冲突。`npm run verify` 40/40 通过，安装器退出码为 0，安装目录主程序与本次 release 构建产物 SHA-256 一致；`D:\弹阅` 仅一个运行进程，8 本书和 7643 个片段保持完整。
- 2026-08-28 已覆盖安装阅读标题精简版：弹窗和连续阅读移除模板桥接小字，正文首句衍生的伪标题不再渲染；真实章节标题仅在章节首次出现且正文未重复包含时展示。`npm run verify` 41/41 通过，安装器退出码为 0且安装目录主程序与本次 release 构建一致；`D:\弹阅` 仅一个运行进程，8 本书和 7643 个片段保持完整。
- 2026-08-28 已覆盖安装“继续阅读”切换竞态修正版：去除弹窗侧重复状态广播与提前显示主窗口，由主窗口在连续阅读层准备完成后自行显示，并在状态刷新时保留活动阅读层。`npm run verify` 41/41 通过，安装器退出码为 0且安装目录主程序与本次 release 构建一致；安装前后数据库均为 7 本书、6286 个片段，仅一个运行进程。
- 2026-08-28 已覆盖安装首页单一当前书与书架排序版：首页只渲染当前正在阅读的一本；书架当前书在四种排序下固定置顶并带“正在读”标识，排序只重排界面节点、不写阅读状态。`npm run verify` 42/42 通过，安装器退出码为 0且安装目录主程序与本次 release 构建一致；数据库仍为 7 本书、6286 个片段，仅一个运行进程和一条卸载记录。
- 2026-08-28 已覆盖安装显式阅读目标版：每本书按自己的历史游标计算接续片段，书架和详情页均明确展示具体段号与章节；非当前书先确认或选择位置，任意片段点击会先保存为当前阅读目标再打开正文。`npm run verify` 43/43 通过，安装器退出码为 0且安装目录主程序与本次 release 构建一致；数据库仍为 7 本书、6286 个片段，仅一个运行进程和一条卸载记录。
- 2026-08-31 已完成 0.2.1 干净内容发行链路：首次安装仅生成完整《道德经》与五卷正文版《毛泽东选集》，合计 6 本书、6284 个未读片段，不携带开发机的进度、收藏、笔记或设置；种子包逐本经过协议、顺序和 100% 覆盖复核，重复初始化不会重复添加，既有 SQLite 覆盖升级不注入或覆盖种子。`npm run verify` 45/45、`cargo fmt --check`、`cargo check` 与 NSIS 构建通过；0.2.1 已覆盖安装到 `D:\弹阅` 且仅一个进程和一条卸载记录。发行命令支持证书指纹签名并固定 SHA-256 时间戳配置，但本机构建时无代码签名证书，本次安装包为未签名并附 SHA-256 校验文件。
- 2026-08-31 已收口弹窗关闭后的接续语义：右上角手动关闭和倒计时自然收起均记录 `dismissedAt`、计入书籍已读进度并持久化推进到下一段；稍后提醒、今天暂停、打开首页和展开连续阅读不会误推进。底层仍保留 `shown` 与主动 `confirmed` 的行为区别。`npm run verify` 46/46 通过。
- 2026-08-31 已完成 0.2.2《毛主席语录》正文版：从结构化页面提取 33 章 427 条语录，只保留语录正文，删除独立出处、日期、卷页、编者标注和正文注号；生成 427 个原文块、528 个片段、56417 个 Unicode 标量，覆盖率 100%。旧的 500 段含出处版本会在应用启动时通过领域层迁移移除，新的正文版幂等加入，并保持用户当前在读书籍和片段不变。
- 2026-08-31 SQLite 升级到 schema v2，持久化 `contentSeedVersion`；旧 v1 数据库原位增加默认值 0 后再由应用执行一次内容升级，避免每次启动重复迁移。内容版本提升时会先把升级前完整状态原子写入应用数据目录 `backups`，再事务保存新状态。Rust 18/18、前端 47/47 通过；构建版本信息改为从 `package.json` 单一读取。
- 2026-08-31 已生成并覆盖安装 0.2.2 到 `D:\弹阅`：主程序、CLI、安装器文件版本均为 0.2.2；SQLite 实际从 v1 升级到 v2，书架由 8 本/7312 段收敛为 7 本/6812 段，只保留《毛主席语录（正文版）》528 段，当前书与第 9 段游标保持不变。升级前 8 本/7312 段状态已备份为 12463180 字节 JSON；重启后书目、游标和备份数量不变。最终安装器 SHA-256 为 `0e318e8916effebe56552ab5ca835a26be3cd0b040d4c8ab67f4e81a1cc38c14`，仍为未签名测试包；`release` 目录只保留 0.2.2，旧 0.2.1 安装器可恢复地移入 `archive/obsolete-installers`。
- 2026-08-31 v0.2.3 优化书皮题签：不再截取前两个字，短书名完整显示，长书名最多两行，《毛泽东选集》主书名与卷次分行；书架、首页、收藏和紧凑题签使用各自字号，保持原有尺寸、纹理与信息密度。前端 48/48、`cargo fmt --check`、`cargo check` 和 Windows NSIS 构建通过；已覆盖安装并启动 `D:\弹阅\tanyue.exe` 0.2.3。安装器 SHA-256 为 `a35e6b0c5591ac50b3a0e7f6fed9cb44cfde6f294aa6e763d5e22d8105618fcb`，仍为未签名测试包；`release` 目录只保留 0.2.3，0.2.2 已可恢复地移入 `archive/obsolete-installers`。
- 已从安装目录执行 `tanyue-cli schema`、`validate` 和 `import`；运行中的安装版在约 2 秒内接收样例包、完成复核并清空 pending，未产生 rejected 文件。
- 2026-08-28 使用安装在 `D:\弹阅` 的 CLI 完成《毛泽东选集 第一卷》真实 PDF 导入：从 352 页老式中文 PDF 中保留书内第 3～340 页正文和正文注释，生成 18 篇、337 个来源块、1357 个约 30 秒微阅读片段，共 217665 个 Unicode 标量；CLI 与应用复核覆盖率均为 100%，无替代字符、无私用区字符、pending/rejected 均为空，SQLite 中书籍和片段数为 1/1357。
- 本次可重复导入包位于 `D:\tanyue\output\imports\毛泽东选集-第一卷.tanyue.json`，来源 SHA-256 为 `d704b7f104b5db118f9249522e472532cfd66bf14232d81066860f6059bb4940`。
- 2026-08-28 从 `weiyinfu/MaoZeDongAnthology` 提交 `f23ff5c48d976561f888c6ce8c594725d5670e38` 生成五卷正文版：229 篇，删除文末注释 182451 字符、篇首编者说明 20207 字符和正文注号 1086 个，保留正文 986563 个 Unicode 标量，拆成 6203 个约 30 秒片段。五卷均经安装版 CLI 和应用双重复核为 100% 覆盖，pending/rejected 均为空。
- 五卷正文版包与清洗报告位于 `D:\tanyue\output\imports\mao-anthology-body-only`；转换脚本为 `D:\tanyue\tools\create-mao-anthology-import.py`。SQLite 已同时保留原 PDF 第一卷及新导入的五卷正文版，合计 7560 个《毛选》片段。
- 本轮覆盖安装后再次核对 SQLite：原 PDF 第一卷 1357 段与五卷正文版 6203 段均在库，另有《道德经》81 段和网页演示 2 段；共 8 本、7643 段，Agent pending/rejected 均为空。
- 已安装应用可启动并响应，SQLite 文件位于 `%APPDATA%\com.generalconsulting.tanyue\tanyue.sqlite3`。
- 独立微读弹窗采用正文优先布局：无透明外边距、四操作单行；默认在 480～600px 宽和 340～720px 高之间自适应，也可限制为紧凑 520×560 或小窗 480×460 上限。
- 弹窗内操作反馈已收敛为紧邻操作区的轻量提示：不遮挡正文、1.5 秒消失，连续操作不堆叠。
- 桌面悬浮入口的逻辑尺寸为 52×52，仅保留 48×48 品牌图标并移除含义不明的数字角标；针对 Windows 的顶层窗口最小宽度限制，额外用原生窗口区域裁掉左右透明命中区；阅读卡片打开时入口继续可见，关闭卡片后无需重新创建或恢复图标。
- 弹窗与悬浮图标改由 Rust `WindowCoordinator` 统一管理：打开前冻结物理坐标锚点，所有自适应测量复用同一锚点；拖动悬浮球时卡片实时跟随，拖动卡片时悬浮球反向跟随，关闭后保持最后联动坐标。
- 弹窗默认从悬浮图标向屏幕内侧展开，图标中心与 50px 标题栏中心对齐；首次展开先应用并读取实际外框尺寸，再以 4px 安全净距定位，两者不重叠，仅在屏幕可用区不足时做边界夹取。
- 20 万字单块文本导入覆盖率 100%，来源区间总长与标准化原文一致。
- 《道德经》81 章无漏段、无重复、顺序连续测试通过。
- 静态构建成功生成 `dist/`，路径正确为 `D:\tanyue\dist`。
- `apple.css`、悬浮入口、四操作弹窗和桌面命令被工程完整性脚本检查。
- SVG、PNG、ICO、ICNS 图标文件已生成并通过格式识别。
- Chromium 已完成主界面、弹窗、悬浮图标和连续阅读面板渲染截图。
- 最终压缩包会执行结构校验和 ZIP 完整性检查。

## 本环境未完成的验证

- 记事本持续输入时的弹窗不抢焦点仍需人工观察确认。
- 托盘菜单“退出”、开机启动清理、睡眠两小时恢复、多显示器/缩放、DND 尚未完成端到端测试。
- 安装包未签名，未接入自动升级；macOS 尚未真机验证。
- 内置《毛泽东选集》和《毛主席语录》的公开再分发授权已由项目所有者确认；软件 MIT 许可与内置文本权利范围分开说明。
- 复杂 EPUB/PDF/DOCX/URL 的解析质量由调用 Agent 和其解析工具负责；弹阅只保证标准包校验与导入，不承诺替外部工具修复 OCR、DRM、多栏顺序或网页反爬。

## 本地接手的第一道硬门禁

```bash
npm install
npm run verify
cargo check --manifest-path src-tauri/Cargo.toml
npm run tauri:dev
```

随后必须逐项验证：

1. 悬浮图标显示、隐藏和点击。
2. 微读弹窗不抢当前输入焦点。
3. 弹窗关闭后悬浮图标恢复。
4. 多显示器和缩放比例下的位置。
5. 关闭主窗口后托盘和调度仍工作。
6. 关闭“显示桌面悬浮图标”后不残留窗口。
